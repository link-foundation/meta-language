# arithmetic grammar with counted repetition and character classes
root ::= expr
expr ::= term (("+" | "-") term)*
term ::= factor (("*" | "/") factor)*
factor ::= number | "(" expr ")"
number ::= [0-9]{1,} ("." [0-9]{1,3})?
identifier ::= [a-zA-Z_] [a-zA-Z0-9_]{0,}
not_quote ::= [^"\n]

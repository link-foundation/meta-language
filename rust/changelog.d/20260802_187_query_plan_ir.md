---
bump: minor
---

### Added
- Add a shared Rust/JavaScript executable query-plan IR with SQL CRUD lowering,
  vendor-profile normalization, CST provenance, semantic links, fail-closed
  validation, and pluggable non-SQL frontends.

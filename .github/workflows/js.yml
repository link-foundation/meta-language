name: JavaScript

on:
  push:
    branches:
      - main
    paths:
      - 'js/**'
      - 'parity/**'
      - '.secretlintrc.json'
      - '.github/workflows/js.yml'
  pull_request:
    types: [opened, synchronize, reopened]
    paths:
      - 'js/**'
      - 'parity/**'
      - '.secretlintrc.json'
      - '.github/workflows/js.yml'
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      release_version:
        description: 'Expected package version (optional safety check)'
        required: false
        type: string

permissions:
  contents: read

env:
  GIT_CONFIG_COUNT: '1'
  GIT_CONFIG_KEY_0: init.defaultBranch
  GIT_CONFIG_VALUE_0: main

jobs:
  test:
    name: JavaScript Package
    runs-on: ubuntu-latest
    timeout-minutes: 10
    concurrency:
      group: check-${{ github.workflow }}-${{ github.ref }}-javascript-test
      cancel-in-progress: true
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Simulate fresh merge with base branch
        if: github.event_name == 'pull_request'
        env:
          BASE_REF: ${{ github.base_ref }}
        run: bash js/scripts/simulate-fresh-merge.sh

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: js/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: js

      - name: Run tests
        run: npm test
        working-directory: js

      - name: Check Rust/JavaScript parity manifest
        run: npm run check:parity
        working-directory: js

      - name: Verify npm package contents
        run: npm pack --dry-run
        working-directory: js

      - name: Scan repository for secrets
        run: npx --yes -p secretlint -p @secretlint/secretlint-rule-preset-recommend secretlint --secretlintignore .gitignore "**/*"

  publish:
    name: Publish npm Package
    needs: test
    if: |
      !cancelled() && needs.test.result == 'success' && (
        github.event_name == 'workflow_dispatch' ||
        (github.event_name == 'release' && github.event.action == 'published')
      )
    runs-on: ubuntu-latest
    timeout-minutes: 30
    concurrency:
      group: release-${{ github.repository }}-main-write
      cancel-in-progress: false
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24
          registry-url: 'https://registry.npmjs.org'

      - name: Update npm for trusted publishing
        run: |
          npm install -g npm@11
          npm --version

      # actions/setup-node writes `_authToken=${NODE_AUTH_TOKEN}` and exports the
      # placeholder `XXXXX-XXXXX-XXXXX-XXXXX` whenever no token is supplied. npm
      # then skips the OIDC exchange and the registry rejects the anonymous PUT
      # with a misleading `E404`. Strip that placeholder so trusted publishing is
      # actually attempted; keep it when a real NPM_TOKEN bootstrap secret exists.
      # See actions/setup-node#1551 and issue #191.
      - name: Prepare npm authentication
        id: npm_auth
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
          PACKAGE_NAME: meta-language
          PUBLISH_WORKFLOW_FILE: .github/workflows/js.yml
          # Set to '1' (or add --verbose) to trace npm credential resolution.
          NPM_AUTH_DEBUG: ${{ vars.NPM_AUTH_DEBUG }}
        run: node scripts/prepare-npm-auth.mjs
        working-directory: js

      - name: Read package metadata
        id: package
        working-directory: js
        run: |
          name="$(node -p "require('./package.json').name")"
          version="$(node -p "require('./package.json').version")"

          if [ "$name" != "meta-language" ]; then
            echo "::error::Expected package name meta-language, got $name"
            exit 1
          fi

          echo "name=$name" >> "$GITHUB_OUTPUT"
          echo "version=$version" >> "$GITHUB_OUTPUT"
          echo "Package: $name@$version"

      - name: Verify requested release version
        env:
          PACKAGE_VERSION: ${{ steps.package.outputs.version }}
          RELEASE_TAG: ${{ github.event.release.tag_name }}
          REQUESTED_VERSION: ${{ github.event.inputs.release_version }}
        run: |
          if [ -n "$RELEASE_TAG" ]; then
            normalized_tag="${RELEASE_TAG#v}"
            if [ "$normalized_tag" != "$PACKAGE_VERSION" ]; then
              echo "::error::Release tag $RELEASE_TAG does not match js/package.json version $PACKAGE_VERSION"
              exit 1
            fi
          fi

          if [ -n "$REQUESTED_VERSION" ] && [ "$REQUESTED_VERSION" != "$PACKAGE_VERSION" ]; then
            echo "::error::Requested version $REQUESTED_VERSION does not match js/package.json version $PACKAGE_VERSION"
            exit 1
          fi

      - name: Check npm registry
        id: npm
        env:
          PACKAGE_VERSION: ${{ steps.package.outputs.version }}
        working-directory: js
        run: |
          if npm view "meta-language@$PACKAGE_VERSION" version >/dev/null 2>&1; then
            echo "already_published=true" >> "$GITHUB_OUTPUT"
          else
            echo "already_published=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Publish to npm
        if: steps.npm.outputs.already_published != 'true'
        # Optional NPM_TOKEN bootstrap fallback. OIDC trusted publishing is the
        # steady-state mechanism; the token path only matters until a trusted
        # publisher is configured for the package on npmjs.com.
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: npm publish --provenance
        working-directory: js

      - name: Explain npm publish failure
        if: failure() && steps.npm.outputs.already_published != 'true'
        env:
          AUTH_MODE: ${{ steps.npm_auth.outputs.auth_mode }}
        run: |
          echo "::error::npm publish failed with auth mode '$AUTH_MODE'."
          if [ "$AUTH_MODE" = "oidc" ]; then
            echo "An E404 on PUT https://registry.npmjs.org/meta-language means the registry"
            echo "treated the request as anonymous. Confirm that meta-language has a trusted"
            echo "publisher on npmjs.com pointing at ${{ github.repository }} and workflow file"
            echo ".github/workflows/js.yml, or set an NPM_TOKEN secret to bootstrap the publish."
          fi

      - name: Verify published package
        if: steps.npm.outputs.already_published != 'true'
        env:
          PACKAGE_VERSION: ${{ steps.package.outputs.version }}
        run: npm view "meta-language@$PACKAGE_VERSION" version
